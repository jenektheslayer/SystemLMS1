spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lms
    username: admin
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yml

